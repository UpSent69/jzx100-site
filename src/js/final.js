import { revealLines } from "./motion.js";

/**
 * Экран 6. Крупный кадр, название, контакты. Заглушка: финальных материалов
 * от заказчика ещё нет.
 */
export function initFinal(root) {
  revealLines(root, { stagger: 0.1 });
}
